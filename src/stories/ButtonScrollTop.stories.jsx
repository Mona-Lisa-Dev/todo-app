import ButtonScrollTop from 'components/ButtonScrollTop';

export default {
  component: ButtonScrollTop,
  title: 'Components/ButtonScrollTop',
};

const Template = args => <ButtonScrollTop {...args} />;

export const Default = Template.bind({});

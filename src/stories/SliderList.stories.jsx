import SliderList from 'components/SliderList';

export default {
  component: SliderList,
  title: 'Components/SliderList',
};

const Template = args => <SliderList {...args} />;
export const Default = Template.bind({});

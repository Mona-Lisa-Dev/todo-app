import AdminPanel from 'components/AdminPanel';

export default {
  component: AdminPanel,
  title: 'Components/AdminPanel',
};

// const Template = args => <AdminPanel {...args} />;

// export const Default = Template.bind({});
